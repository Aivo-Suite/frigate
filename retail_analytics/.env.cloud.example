# Copy to .env; keep existing production values when updating a deployment.
# This file contains placeholders only. Do not commit the actual .env.
AIVO_POSTGRES_PASSWORD=REPLACE_WITH_EXISTING_DATABASE_PASSWORD
# URL-encode reserved characters in the password component.
AIVO_DATABASE_URL=postgresql://aivo:URL_ENCODED_PASSWORD@saas-db:5432/aivo_saas
